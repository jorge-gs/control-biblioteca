<Page
    x:Class="Biblioteca.Carreras"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Biblioteca"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="ColumnaCarreras" MinWidth="240" Width="*" />
            <ColumnDefinition x:Name="ColumnaClases" Width="240" />
            <ColumnDefinition x:Name="ColumnaInformacion" Width="240" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition x:Name="FilaContenido" Height="*" />
            <RowDefinition x:Name="FilaBarra" Height="60" />
        </Grid.RowDefinitions>

        <RelativePanel Grid.Column="0" Grid.Row="0" Background="Azure" />
        <RelativePanel Grid.Column="0" Grid.Row="1" Background="Beige" />
        <RelativePanel Grid.Column="0" Grid.Row="2" Background="Azure" />

        <RelativePanel Grid.Column="0" Grid.ColumnSpan="4" Grid.Row="1" Background="Aqua" />


    </Grid>
</Page>
