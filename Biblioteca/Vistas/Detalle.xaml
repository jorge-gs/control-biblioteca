<Page
    x:Class="Biblioteca.Detalle"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Biblioteca"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="ColumnaLista" MinWidth="240" Width="2*" />
            <ColumnDefinition x:Name="ColumnaInformacion" MinWidth="240" Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition x:Name="FilaContenido" Height="*" />
            <RowDefinition x:Name="FilaBarra" Height="42" />
        </Grid.RowDefinitions>

        <Frame x:Name="FrameLista" Grid.Column="0" Grid.Row="0" />
        <Frame x:Name="FrameInformacion" Grid.Column="1" Grid.Row="0" />

        <RelativePanel x:Name="Botones" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" >
            <Button x:Name="Agregar" Margin="5" >Agregar</Button>
            <AutoSuggestBox x:Name="Buscar" Margin="5" Width="240" RelativePanel.RightOf="Agregar" QueryIcon="Find" PlaceholderText="Buscar" />
            <Button x:Name="Eliminar" Margin="5" RelativePanel.AlignRightWithPanel="True" >Eliminar</Button>
        </RelativePanel>


    </Grid>
</Page>
